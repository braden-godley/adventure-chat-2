import { Message } from "@/store";

const MessageHistory: React.FC<{ history: Array<Message> }> = ({ history }) => {
    return (
        <div>
            test
        </div>
    );
};

export default MessageHistory;
